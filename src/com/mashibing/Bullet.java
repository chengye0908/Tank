package com.mashibing;import java.awt.Color;import java.awt.Graphics;/** * 抽象一个子弹类,封装子弹绘画方法 * @author Administrator * */public class Bullet {	private int x,y;	private Dir dir;	private static  int SPEED = 10;	private static  int WIDTH = 50;	private static  int HEIGHT = 50;	private  boolean islive = true;	TankFrame tf = null;	public Bullet(int x, int y, Dir dir,TankFrame tf) {		super();		this.x = x;		this.y = y;		this.dir = dir;		this.tf = tf;	}	/**	 * 每创建一个窗口则自动调用该方法	 * Frame自带方法,继承过来	 */	public void paint(Graphics g) {		System.out.println(islive);		if(!islive) tf.bulletList.remove(this);		//画一个子弹//		Color c = g.getColor();//		g.setColor(c.RED);//		g.fillOval(x, y, WIDTH, HEIGHT);		switch(dir) {		case LEFT:		//	x = x - SPEED;			g.drawImage(ResoureMgr.bulletL,x,y,null);			x = x - SPEED;			break;		case RIGHT:			g.drawImage(ResoureMgr.bulletR,x,y,null);			x += SPEED;			break;		case UP:			g.drawImage(ResoureMgr.bulletU,x,y,null);			y = y - SPEED;			break;		case DOWN:			g.drawImage(ResoureMgr.bulletD,x,y,null);			y += SPEED;			break;		}		if(x < 0 || y < 0 || x > tf.GAME_WIDTH ||  y > tf.GAME_HEIGHT) this.islive  = false;	}}